---
title: "klim share link open"
description: Install tools from a share token
---

Install tools from a compact share token generated by `klim share link`.

## Usage

```bash
klim share link open <token> [flags]
```

## Arguments

| Argument | Description |
|----------|-------------|
| `<token>` | Share token (starts with `klim:v1:`) |

## Flags

| Flag | Short | Description |
|------|-------|-------------|
| `--yes` | `-y` | Install all tools without prompting |

## Examples

```bash
# Interactive — confirm before installing
klim share link open klim:v1:H4sIAAAA...

# Non-interactive — install all
klim share link open klim:v1:H4sIAAAA... --yes
```

## How It Works

1. Decodes the share token
2. Resolves tools against the local catalog
3. Installs each tool via the best available package manager for the current OS

## See Also

- [`klim share link`](../share/) — Generate a share token
- [`klim share import`](../import/) — Install from a YAML manifest
